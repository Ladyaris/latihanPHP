<?php
    // Operator aritmatika
    $a = 10;
    $b = 5;

    // $c = $a * $b;
    // echo "$c";

    // Operator relation
    // $d = $a > $b;
    // echo "$a > $b : $d";

    // $d = $a < $b;
    // echo "$a < $b : $d";

    // $d = $a == $b;
    // echo "$a == $b : $d";

    // $d = $a != $b;
    // echo "$a != $b : $d";

    // $d = $a <= $b;
    // echo "$a <= $b : $d";

    // $d = $a >= $b;
    // echo "$a >= $b : $d";

    // $d = $a <= $b;
    // echo "$a <= $b : $d";

    // Operator logika
    $x = 10;
    // var_dump($x > 20 || $x %2 == 0); 

    var_dump($x >! 20);
?>